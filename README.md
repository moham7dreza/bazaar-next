### Advertisement shop made with next js
frontend of [bazaar-laravel](https://github.com/moham7dreza/bazaar-laravel) project in my repo
### setup
1. copy .env.example as .env and apply changes if needed
2. run `npm install && npm run dev` in your terminal
3. goto http://localhost:3000 in your browser to see result